{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "root",
  "password": "root",
  "database": "device",
  "synchronize": true,
  "entities": ["src/modules/**/*.model.ts"],
  "migrations": ["src/migration/**/*.ts"],
  "cli": {
    "migrationsDir": "migration"
  }
}
